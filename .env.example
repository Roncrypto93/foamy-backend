PORT=3000

# Endpoint API Open-Meteo (non richiedono API key per uso non commerciale)
OPEN_METEO_FORECAST_URL=https://api.open-meteo.com/v1/forecast
OPEN_METEO_MARINE_URL=https://marine-api.open-meteo.com/v1/marine

# Correzione manuale (nodi) sommata alla sola velocità media del vento
# (non alle raffiche) nelle condizioni attuali (GET /api/forecast/:spotId),
# non nel forecast settimanale. Taratura da confronto con anemometro sul
# campo (luglio 2026): il dato Open-Meteo risultava sistematicamente più
# basso della realtà. Ricontrollare periodicamente se resta accurata.
WIND_SPEED_CORRECTION_KN=0.5

# Modello Open-Meteo per le SOLE raffiche (condizioni attuali e forecast
# settimanale). Default gfs_seamless: taratura allineata a Windguru (GFS),
# perché le raffiche ICON risultavano sistematicamente più alte (confronto
# luglio 2026, es. 17.1kn ICON vs 12.2kn GFS nello stesso istante).
# Velocità/direzione restano dal modello principale (ICON-EU / best_match).
WIND_GUST_MODEL=gfs_seamless

# TTL della cache forecast, in secondi (usato sia per la cache in-memory
# locale sia per Upstash Redis sotto). Default 10800 = 180 minuti, per
# stare ampiamente dentro la quota gratuita giornaliera di Open-Meteo.
FORECAST_CACHE_TTL=10800

# Cache persistente (Upstash Redis, via REST — nessuna libreria client
# necessaria). Opzionale: se non impostate, si usa solo la cache in-memory
# (che su Render free si azzera ad ogni riavvio/sleep del servizio, quindi
# senza Upstash la quota Open-Meteo si esaurisce più facilmente). Account
# gratuito su upstash.com > Redis > Create Database > REST API.
# UPSTASH_REDIS_REST_URL=https://xxxxx.upstash.io
# UPSTASH_REDIS_REST_TOKEN=xxxxx
UPSTASH_TIMEOUT_MS=5000

# Copernicus Marine (CMEMS) - script Python invocato come sottoprocesso.
# Le credenziali NON vanno qui: si impostano una volta sola sulla macchina
# che esegue il backend con `copernicusmarine login` (salvate in
# $HOME/.copernicusmarine-credentials), oppure via le env ufficiali del
# toolbox COPERNICUSMARINE_SERVICE_USERNAME / COPERNICUSMARINE_SERVICE_PASSWORD.
PYTHON_BIN=python3
COPERNICUS_TIMEOUT_MS=20000
# Opzionale: forza un dataset_id specifico (vedi scripts/fetch_copernicus_wave.py).
# COPERNICUS_DATASET_ID=cmems_mod_med_wav_anfc_4.2km_PT1H-i

# Usato SOLO dal forecast settimanale (GET /api/forecast/:spotId/daily):
# URL del microservizio foamy-copernicus (Flask, dataset anfc con orizzonte
# di forecast reale). Le condizioni attuali non usano questa variabile.
COPERNICUS_SERVICE_URL=http://localhost:5000
# Timeout per singola chiamata a foamy-copernicus (ms). Più alto del timeout
# del subprocess Python perché ogni chiamata riapre un dataset CMEMS da zero,
# lento su hosting free-tier.
COPERNICUS_DAILY_TIMEOUT_MS=45000
# Il forecast copre 7 giorni, ma Copernicus reale viene interrogato solo per
# i primi N (sequenziale, 15-45s a chiamata: farlo per tutti e 7 porterebbe
# il caso peggiore a ~5 minuti). Oltre questo limite si usa solo ECMWF.
COPERNICUS_REAL_DAYS=3

# Usato SOLO da GET /api/forecast/:spotId/tide (marea + temperatura acqua).
# Timeout per la chiamata a foamy-copernicus /tide (scarica l'intera serie
# oraria del giorno, non un singolo istante come /wave).
COPERNICUS_TIDE_TIMEOUT_MS=45000
# Temperatura acqua (Open-Meteo Marine): aggiornata al massimo ogni 12 ore
# per spot, in secondi. Marea (Copernicus SSH): scaricata una volta al
# giorno per spot; il TTL qui è solo un margine di sicurezza oltre le 24h
# (la chiave di cache include già la data, quindi cambia da sola a mezzanotte).
WATER_TEMP_CACHE_TTL=43200
TIDE_CACHE_TTL=93600
